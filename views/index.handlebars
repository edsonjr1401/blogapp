<div class="p-5 mb-4 bg-body-tertiary rounded-3">
  <div class="container-fluid py-5">
    <h1 class="display-5 fw-bold">Bem-vindo ao Blog app</h1>
    <p class="col-md-8 fs-4">
      Usando classes utilitárias, podemos criar um bloco de destaque como nas versões anteriores.
    </p>
    <button class="btn btn-primary btn-lg" type="button">Crie uma conta</button>
  </div>
</div>

<hr>

<h2>Postagens recentes: </h2>


{{#each Postagens}}
<div class="card">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
    </div>
</div>
{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}    